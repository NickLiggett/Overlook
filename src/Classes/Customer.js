class Customer {
    constructor(id, name) {
        this.id = id
        this.name = name
    }
    selectDate(date) {
        //should show a list of room details 
        //for only rooms that are available on that date
    }
}

export default Customer